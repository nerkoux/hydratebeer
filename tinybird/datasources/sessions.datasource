SCHEMA >
    `id` String,
    `projectId` String,
    `startTime` DateTime64(3),
    `endTime` DateTime64(3),
    `duration` UInt32,
    `pageViews` UInt16,
    `eventCount` UInt32,
    `userAgent` String,
    `browser` String,
    `os` String,
    `device` String,
    `country` String,
    `city` String,
    `referrer` String,
    `landingPage` String,
    `exitPage` String

ENGINE "ReplacingMergeTree"
ENGINE_VER "endTime"
ENGINE_SORTING_KEY "projectId, startTime, id"
